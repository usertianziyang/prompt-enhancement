/**
 * Prompt enhancement UI plugin, node half. The browser implementation is
 * published through the package's `./client` export.
 */

/** Host plugin body for this browser-only UI surface. */
export function apply(): void {}
